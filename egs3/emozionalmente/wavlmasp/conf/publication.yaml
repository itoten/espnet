##########################################################
#                  PUBLICATION SETTING                   #
##########################################################
data_dir: ${oc.env:EMOZIONALMENTE_WAVLMASP_OUTPUT,./data}

pack_model:
  exclude:
    - inference
    - inference_*
    - demo
    - last.ckpt
    - "**/step*.ckpt"
    - "**/epoch*.ckpt"
    - "**/*.log"
    - "**/tensorboard/**"
    - "**/wandb/**"

upload_model:
  hf_repo: espnet/emozionalmente_wavlmasp
